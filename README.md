# Priority App

Private source repository for the Android app shown to users as **Priority Tasks 2**.

- Android package: `com.ghosttools.prioritytasks`
- Supabase project: `yejrnrbydvlwynjehuhp`
- Release signing identity must remain unchanged.
- Signing keys and passwords must never be committed.

The repository name `RANKDO3` is internal only and does not change the app name on the phone.
